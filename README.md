# Trick List

A trick tracking application with TypeScript source code and built output.

## Project Structure

- `/src/` - TypeScript source code from trick-list-source
- `/assets/` - Static assets
- `/api/` - API endpoints
- Root level - Built/compiled files for deployment

## Development

Source code is located in the `src/` directory. The root level contains the built application files.
